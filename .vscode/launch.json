{
  "version": "0.2.0",
  "configurations": [
     {
        "type": "node-terminal",
        "name": "Dev server",
        "request": "launch",
        "command": "bun run dev",
        "cwd": "${workspaceFolder}"
     }
  ]
}